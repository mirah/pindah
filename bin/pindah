#!/usr/bin/env ruby

require File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib', 'pindah_cli'))

begin
  PindahCLI.create *ARGV
rescue ArgumentError
  abort "Usage: #{File.basename($0)} org.example.hello [/path/to/hello_world] [HelloActivity]"
end
